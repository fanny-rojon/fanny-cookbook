<div class="website-header">
  <h1 class="index-title">Browse by Category</h1>
</div>
<% if user_signed_in? %>
  <div class="margin-links">
    <%= link_to "new category", new_category_path, class: "website-button button-white" %>
    <%= link_to "new recipe", new_recipe_path, class: "website-button button-white" %>
  </div>
<% end %>
<div class="container">
  <div class="grid-wrapper">
    <%= render @categories %>
  </div>
</div>
